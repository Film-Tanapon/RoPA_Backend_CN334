services:
  api-server:
    build: .
    ports:
      - "3340:8000"
    volumes:
      - .:/app
    env_file:
      - .env

  robot_tester:
    build:
      context: ./tests
      dockerfile: Dockerfile.robot
    volumes:
      - ./tests:/tests
    depends_on:
      - api-server